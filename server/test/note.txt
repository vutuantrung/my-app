New-NetFirewallRule `
  -DisplayName "Express-WS-3124" `
  -Direction Inbound `
  -Protocol TCP `
  -LocalPort 3124 `
  -Profile Private `
  -Action Allow

Get-NetFirewallRule -DisplayName "Express-WS-3124" |
  Format-List DisplayName,Enabled,Profile,Direction,Action

Get-NetFirewallRule -DisplayName "Express-WS-3124" |
Get-NetFirewallPortFilter |
Format-List LocalPort,Protocol

netstat -ano | findstr ":3124"

lets create a new ws server running on port 3124, whenever something changes (new movie, new idol, news, systems...), the server can broadcast to all its subcribers, the format will be json with properties(id, messageType, title, createdAt, data)