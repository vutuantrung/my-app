import { $, $$ } from "../utils/dom.js"; import { setActivePage } from "../core/router.js";
export function initSearch({ setStatus }){ const form=$("#searchForm"); const input=$("#searchInput"); const btn=$("#searchBtn"); function runSearch(query){ const q=(query??"").trim(); if(!q) return; setStatus?.(`Searching: "${q}"…`, 800); setActivePage("library-all"); } form?.addEventListener("submit",(e)=>{ e.preventDefault(); runSearch(input?.value); }); btn?.addEventListener("click",(e)=>{ e.preventDefault(); runSearch(input?.value); }); document.addEventListener("keydown",(e)=>{ const isMac=navigator.platform.toUpperCase().includes("MAC"); if((isMac? e.metaKey : e.ctrlKey) && e.key.toLowerCase()==="k"){ e.preventDefault(); input?.focus(); input?.select(); } }); }