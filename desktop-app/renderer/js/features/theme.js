import { kv } from "../utils/store.js"; import { $ } from "../utils/dom.js";
const THEME_KEY="forced-theme";
function applyThemeFromStorage(btn){ const val=kv.get(THEME_KEY,null); const root=document.documentElement; if(val==="light") root.setAttribute("data-theme","light"); else if(val==="dark") root.setAttribute("data-theme","dark"); else root.removeAttribute("data-theme"); const isForcedLight=root.getAttribute("data-theme")==="light"; const isForcedDark=root.getAttribute("data-theme")==="dark"; const effectiveDark=isForcedDark||(!isForcedLight&&matchMedia("(prefers-color-scheme: dark)").matches); if(btn) btn.textContent=effectiveDark?"Switch to Light theme":"Switch to Dark theme"; }
export function initTheme(){ const btn=$("#themeToggle"); applyThemeFromStorage(btn); matchMedia("(prefers-color-scheme: dark)").addEventListener("change",()=>{ if(kv.get(THEME_KEY,null)==null) applyThemeFromStorage(btn); }); btn?.addEventListener("click",()=>{ const root=document.documentElement; const effectiveDark=root.getAttribute("data-theme")==="dark"||(!root.hasAttribute("data-theme")&&matchMedia("(prefers-color-scheme: dark)").matches); kv.set(THEME_KEY, effectiveDark?"light":"dark"); applyThemeFromStorage(btn); }); }