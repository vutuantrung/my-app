import { $, on } from "../utils/dom.js"; import { setActivePage } from "../core/router.js";
function openMenu(menu, btn){ menu.classList.add("open"); btn.setAttribute("aria-expanded","true"); } function closeMenu(menu, btn){ menu.classList.remove("open"); btn.setAttribute("aria-expanded","false"); }
export function initUserMenu({ setStatus }){ const btn=$("#avatarBtn"); const menu=$("#userMenu"); if(!btn||!menu) return; btn.addEventListener("click",(e)=>{ e.stopPropagation(); menu.classList.contains("open")? closeMenu(menu,btn) : openMenu(menu,btn); }); document.addEventListener("click",(e)=>{ if(!menu.contains(e.target) && e.target!==btn) closeMenu(menu,btn); }); document.addEventListener("keydown",(e)=>{ if(e.key==="Escape") closeMenu(menu,btn); }); menu.addEventListener("click",(e)=>{ const item=e.target.closest(".menu-item"); if(!item) return; const action=item.dataset.action; if(action==="account"){ setStatus?.("Opening account…",800); } else if(action==="settings"){ setActivePage("settings"); } else if(action==="help"){ setStatus?.("Opening help…",800); } }); }